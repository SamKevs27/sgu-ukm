import 'package:flutter/material.dart';

class BemCyclesScreen extends StatelessWidget {
  const BemCyclesScreen({super.key});
  @override
  Widget build(BuildContext context) =>
      const Center(child: Text('BEM Cycles — coming soon'));
}