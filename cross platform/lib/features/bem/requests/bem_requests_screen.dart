import 'package:flutter/material.dart';

class BemRequestsScreen extends StatelessWidget {
  const BemRequestsScreen({super.key});
  @override
  Widget build(BuildContext context) =>
      const Center(child: Text('BEM Requests — coming soon'));
}